# Test fixtures

Fixtures in this directory must be synthetic and must not contain production exports, personal data, secrets, or live service credentials. `synthetic-tournament.json` is the neutral seed for later adapter and API tests.
